// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package tenant

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// IncomeSaleItem is an object representing the database table.
type IncomeSaleItem struct {
	ID           int64         `boil:"id" json:"id" toml:"id" yaml:"id"`
	IncomeSaleID null.Int64    `boil:"income_sale_id" json:"income_sale_id,omitempty" toml:"income_sale_id" yaml:"income_sale_id,omitempty"`
	ProductID    null.Int64    `boil:"product_id" json:"product_id,omitempty" toml:"product_id" yaml:"product_id,omitempty"`
	Amount       types.Decimal `boil:"amount" json:"amount" toml:"amount" yaml:"amount"`
	PriceCost    types.Decimal `boil:"price_cost" json:"price_cost" toml:"price_cost" yaml:"price_cost"`
	Price        types.Decimal `boil:"price" json:"price" toml:"price" yaml:"price"`
	Discount     types.Decimal `boil:"discount" json:"discount" toml:"discount" yaml:"discount"`
	TypeDiscount string        `boil:"type_discount" json:"type_discount" toml:"type_discount" yaml:"type_discount"`
	Subtotal     types.Decimal `boil:"subtotal" json:"subtotal" toml:"subtotal" yaml:"subtotal"`
	Total        types.Decimal `boil:"total" json:"total" toml:"total" yaml:"total"`
	CreatedAt    time.Time     `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *incomeSaleItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L incomeSaleItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var IncomeSaleItemColumns = struct {
	ID           string
	IncomeSaleID string
	ProductID    string
	Amount       string
	PriceCost    string
	Price        string
	Discount     string
	TypeDiscount string
	Subtotal     string
	Total        string
	CreatedAt    string
}{
	ID:           "id",
	IncomeSaleID: "income_sale_id",
	ProductID:    "product_id",
	Amount:       "amount",
	PriceCost:    "price_cost",
	Price:        "price",
	Discount:     "discount",
	TypeDiscount: "type_discount",
	Subtotal:     "subtotal",
	Total:        "total",
	CreatedAt:    "created_at",
}

var IncomeSaleItemTableColumns = struct {
	ID           string
	IncomeSaleID string
	ProductID    string
	Amount       string
	PriceCost    string
	Price        string
	Discount     string
	TypeDiscount string
	Subtotal     string
	Total        string
	CreatedAt    string
}{
	ID:           "income_sale_items.id",
	IncomeSaleID: "income_sale_items.income_sale_id",
	ProductID:    "income_sale_items.product_id",
	Amount:       "income_sale_items.amount",
	PriceCost:    "income_sale_items.price_cost",
	Price:        "income_sale_items.price",
	Discount:     "income_sale_items.discount",
	TypeDiscount: "income_sale_items.type_discount",
	Subtotal:     "income_sale_items.subtotal",
	Total:        "income_sale_items.total",
	CreatedAt:    "income_sale_items.created_at",
}

// Generated where

var IncomeSaleItemWhere = struct {
	ID           whereHelperint64
	IncomeSaleID whereHelpernull_Int64
	ProductID    whereHelpernull_Int64
	Amount       whereHelpertypes_Decimal
	PriceCost    whereHelpertypes_Decimal
	Price        whereHelpertypes_Decimal
	Discount     whereHelpertypes_Decimal
	TypeDiscount whereHelperstring
	Subtotal     whereHelpertypes_Decimal
	Total        whereHelpertypes_Decimal
	CreatedAt    whereHelpertime_Time
}{
	ID:           whereHelperint64{field: "\"income_sale_items\".\"id\""},
	IncomeSaleID: whereHelpernull_Int64{field: "\"income_sale_items\".\"income_sale_id\""},
	ProductID:    whereHelpernull_Int64{field: "\"income_sale_items\".\"product_id\""},
	Amount:       whereHelpertypes_Decimal{field: "\"income_sale_items\".\"amount\""},
	PriceCost:    whereHelpertypes_Decimal{field: "\"income_sale_items\".\"price_cost\""},
	Price:        whereHelpertypes_Decimal{field: "\"income_sale_items\".\"price\""},
	Discount:     whereHelpertypes_Decimal{field: "\"income_sale_items\".\"discount\""},
	TypeDiscount: whereHelperstring{field: "\"income_sale_items\".\"type_discount\""},
	Subtotal:     whereHelpertypes_Decimal{field: "\"income_sale_items\".\"subtotal\""},
	Total:        whereHelpertypes_Decimal{field: "\"income_sale_items\".\"total\""},
	CreatedAt:    whereHelpertime_Time{field: "\"income_sale_items\".\"created_at\""},
}

// IncomeSaleItemRels is where relationship names are stored.
var IncomeSaleItemRels = struct {
	Product    string
	IncomeSale string
}{
	Product:    "Product",
	IncomeSale: "IncomeSale",
}

// incomeSaleItemR is where relationships are stored.
type incomeSaleItemR struct {
	Product    *Product    `boil:"Product" json:"Product" toml:"Product" yaml:"Product"`
	IncomeSale *IncomeSale `boil:"IncomeSale" json:"IncomeSale" toml:"IncomeSale" yaml:"IncomeSale"`
}

// NewStruct creates a new relationship struct
func (*incomeSaleItemR) NewStruct() *incomeSaleItemR {
	return &incomeSaleItemR{}
}

func (o *IncomeSaleItem) GetProduct() *Product {
	if o == nil {
		return nil
	}

	return o.R.GetProduct()
}

func (r *incomeSaleItemR) GetProduct() *Product {
	if r == nil {
		return nil
	}

	return r.Product
}

func (o *IncomeSaleItem) GetIncomeSale() *IncomeSale {
	if o == nil {
		return nil
	}

	return o.R.GetIncomeSale()
}

func (r *incomeSaleItemR) GetIncomeSale() *IncomeSale {
	if r == nil {
		return nil
	}

	return r.IncomeSale
}

// incomeSaleItemL is where Load methods for each relationship are stored.
type incomeSaleItemL struct{}

var (
	incomeSaleItemAllColumns            = []string{"id", "income_sale_id", "product_id", "amount", "price_cost", "price", "discount", "type_discount", "subtotal", "total", "created_at"}
	incomeSaleItemColumnsWithoutDefault = []string{"amount", "price_cost", "price", "subtotal", "total"}
	incomeSaleItemColumnsWithDefault    = []string{"id", "income_sale_id", "product_id", "discount", "type_discount", "created_at"}
	incomeSaleItemPrimaryKeyColumns     = []string{"id"}
	incomeSaleItemGeneratedColumns      = []string{}
)

type (
	// IncomeSaleItemSlice is an alias for a slice of pointers to IncomeSaleItem.
	// This should almost always be used instead of []IncomeSaleItem.
	IncomeSaleItemSlice []*IncomeSaleItem
	// IncomeSaleItemHook is the signature for custom IncomeSaleItem hook methods
	IncomeSaleItemHook func(context.Context, boil.ContextExecutor, *IncomeSaleItem) error

	incomeSaleItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	incomeSaleItemType                 = reflect.TypeOf(&IncomeSaleItem{})
	incomeSaleItemMapping              = queries.MakeStructMapping(incomeSaleItemType)
	incomeSaleItemPrimaryKeyMapping, _ = queries.BindMapping(incomeSaleItemType, incomeSaleItemMapping, incomeSaleItemPrimaryKeyColumns)
	incomeSaleItemInsertCacheMut       sync.RWMutex
	incomeSaleItemInsertCache          = make(map[string]insertCache)
	incomeSaleItemUpdateCacheMut       sync.RWMutex
	incomeSaleItemUpdateCache          = make(map[string]updateCache)
	incomeSaleItemUpsertCacheMut       sync.RWMutex
	incomeSaleItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var incomeSaleItemAfterSelectMu sync.Mutex
var incomeSaleItemAfterSelectHooks []IncomeSaleItemHook

var incomeSaleItemBeforeInsertMu sync.Mutex
var incomeSaleItemBeforeInsertHooks []IncomeSaleItemHook
var incomeSaleItemAfterInsertMu sync.Mutex
var incomeSaleItemAfterInsertHooks []IncomeSaleItemHook

var incomeSaleItemBeforeUpdateMu sync.Mutex
var incomeSaleItemBeforeUpdateHooks []IncomeSaleItemHook
var incomeSaleItemAfterUpdateMu sync.Mutex
var incomeSaleItemAfterUpdateHooks []IncomeSaleItemHook

var incomeSaleItemBeforeDeleteMu sync.Mutex
var incomeSaleItemBeforeDeleteHooks []IncomeSaleItemHook
var incomeSaleItemAfterDeleteMu sync.Mutex
var incomeSaleItemAfterDeleteHooks []IncomeSaleItemHook

var incomeSaleItemBeforeUpsertMu sync.Mutex
var incomeSaleItemBeforeUpsertHooks []IncomeSaleItemHook
var incomeSaleItemAfterUpsertMu sync.Mutex
var incomeSaleItemAfterUpsertHooks []IncomeSaleItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *IncomeSaleItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *IncomeSaleItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *IncomeSaleItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *IncomeSaleItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *IncomeSaleItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *IncomeSaleItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *IncomeSaleItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *IncomeSaleItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *IncomeSaleItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddIncomeSaleItemHook registers your hook function for all future operations.
func AddIncomeSaleItemHook(hookPoint boil.HookPoint, incomeSaleItemHook IncomeSaleItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		incomeSaleItemAfterSelectMu.Lock()
		incomeSaleItemAfterSelectHooks = append(incomeSaleItemAfterSelectHooks, incomeSaleItemHook)
		incomeSaleItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		incomeSaleItemBeforeInsertMu.Lock()
		incomeSaleItemBeforeInsertHooks = append(incomeSaleItemBeforeInsertHooks, incomeSaleItemHook)
		incomeSaleItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		incomeSaleItemAfterInsertMu.Lock()
		incomeSaleItemAfterInsertHooks = append(incomeSaleItemAfterInsertHooks, incomeSaleItemHook)
		incomeSaleItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		incomeSaleItemBeforeUpdateMu.Lock()
		incomeSaleItemBeforeUpdateHooks = append(incomeSaleItemBeforeUpdateHooks, incomeSaleItemHook)
		incomeSaleItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		incomeSaleItemAfterUpdateMu.Lock()
		incomeSaleItemAfterUpdateHooks = append(incomeSaleItemAfterUpdateHooks, incomeSaleItemHook)
		incomeSaleItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		incomeSaleItemBeforeDeleteMu.Lock()
		incomeSaleItemBeforeDeleteHooks = append(incomeSaleItemBeforeDeleteHooks, incomeSaleItemHook)
		incomeSaleItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		incomeSaleItemAfterDeleteMu.Lock()
		incomeSaleItemAfterDeleteHooks = append(incomeSaleItemAfterDeleteHooks, incomeSaleItemHook)
		incomeSaleItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		incomeSaleItemBeforeUpsertMu.Lock()
		incomeSaleItemBeforeUpsertHooks = append(incomeSaleItemBeforeUpsertHooks, incomeSaleItemHook)
		incomeSaleItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		incomeSaleItemAfterUpsertMu.Lock()
		incomeSaleItemAfterUpsertHooks = append(incomeSaleItemAfterUpsertHooks, incomeSaleItemHook)
		incomeSaleItemAfterUpsertMu.Unlock()
	}
}

// OneG returns a single incomeSaleItem record from the query using the global executor.
func (q incomeSaleItemQuery) OneG(ctx context.Context) (*IncomeSaleItem, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single incomeSaleItem record from the query.
func (q incomeSaleItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*IncomeSaleItem, error) {
	o := &IncomeSaleItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "tenant: failed to execute a one query for income_sale_items")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all IncomeSaleItem records from the query using the global executor.
func (q incomeSaleItemQuery) AllG(ctx context.Context) (IncomeSaleItemSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all IncomeSaleItem records from the query.
func (q incomeSaleItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (IncomeSaleItemSlice, error) {
	var o []*IncomeSaleItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "tenant: failed to assign all query results to IncomeSaleItem slice")
	}

	if len(incomeSaleItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all IncomeSaleItem records in the query using the global executor
func (q incomeSaleItemQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all IncomeSaleItem records in the query.
func (q incomeSaleItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to count income_sale_items rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q incomeSaleItemQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q incomeSaleItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "tenant: failed to check if income_sale_items exists")
	}

	return count > 0, nil
}

// Product pointed to by the foreign key.
func (o *IncomeSaleItem) Product(mods ...qm.QueryMod) productQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProductID),
	}

	queryMods = append(queryMods, mods...)

	return Products(queryMods...)
}

// IncomeSale pointed to by the foreign key.
func (o *IncomeSaleItem) IncomeSale(mods ...qm.QueryMod) incomeSaleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IncomeSaleID),
	}

	queryMods = append(queryMods, mods...)

	return IncomeSales(queryMods...)
}

// LoadProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incomeSaleItemL) LoadProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSaleItem any, mods queries.Applicator) error {
	var slice []*IncomeSaleItem
	var object *IncomeSaleItem

	if singular {
		var ok bool
		object, ok = maybeIncomeSaleItem.(*IncomeSaleItem)
		if !ok {
			object = new(IncomeSaleItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSaleItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSaleItem))
			}
		}
	} else {
		s, ok := maybeIncomeSaleItem.(*[]*IncomeSaleItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSaleItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSaleItem))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleItemR{}
		}
		if !queries.IsNil(object.ProductID) {
			args[object.ProductID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleItemR{}
			}

			if !queries.IsNil(obj.ProductID) {
				args[obj.ProductID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`products`),
		qm.WhereIn(`products.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Product")
	}

	var resultSlice []*Product
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Product")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for products")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for products")
	}

	if len(productAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Product = foreign
		if foreign.R == nil {
			foreign.R = &productR{}
		}
		foreign.R.IncomeSaleItems = append(foreign.R.IncomeSaleItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ProductID, foreign.ID) {
				local.R.Product = foreign
				if foreign.R == nil {
					foreign.R = &productR{}
				}
				foreign.R.IncomeSaleItems = append(foreign.R.IncomeSaleItems, local)
				break
			}
		}
	}

	return nil
}

// LoadIncomeSale allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incomeSaleItemL) LoadIncomeSale(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSaleItem any, mods queries.Applicator) error {
	var slice []*IncomeSaleItem
	var object *IncomeSaleItem

	if singular {
		var ok bool
		object, ok = maybeIncomeSaleItem.(*IncomeSaleItem)
		if !ok {
			object = new(IncomeSaleItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSaleItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSaleItem))
			}
		}
	} else {
		s, ok := maybeIncomeSaleItem.(*[]*IncomeSaleItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSaleItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSaleItem))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleItemR{}
		}
		if !queries.IsNil(object.IncomeSaleID) {
			args[object.IncomeSaleID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleItemR{}
			}

			if !queries.IsNil(obj.IncomeSaleID) {
				args[obj.IncomeSaleID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`income_sales`),
		qm.WhereIn(`income_sales.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load IncomeSale")
	}

	var resultSlice []*IncomeSale
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice IncomeSale")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for income_sales")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for income_sales")
	}

	if len(incomeSaleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.IncomeSale = foreign
		if foreign.R == nil {
			foreign.R = &incomeSaleR{}
		}
		foreign.R.IncomeSaleItems = append(foreign.R.IncomeSaleItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.IncomeSaleID, foreign.ID) {
				local.R.IncomeSale = foreign
				if foreign.R == nil {
					foreign.R = &incomeSaleR{}
				}
				foreign.R.IncomeSaleItems = append(foreign.R.IncomeSaleItems, local)
				break
			}
		}
	}

	return nil
}

// SetProductG of the incomeSaleItem to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.IncomeSaleItems.
// Uses the global database handle.
func (o *IncomeSaleItem) SetProductG(ctx context.Context, insert bool, related *Product) error {
	return o.SetProduct(ctx, boil.GetContextDB(), insert, related)
}

// SetProduct of the incomeSaleItem to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.IncomeSaleItems.
func (o *IncomeSaleItem) SetProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Product) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"income_sale_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"product_id"}),
		strmangle.WhereClause("\"", "\"", 2, incomeSaleItemPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ProductID, related.ID)
	if o.R == nil {
		o.R = &incomeSaleItemR{
			Product: related,
		}
	} else {
		o.R.Product = related
	}

	if related.R == nil {
		related.R = &productR{
			IncomeSaleItems: IncomeSaleItemSlice{o},
		}
	} else {
		related.R.IncomeSaleItems = append(related.R.IncomeSaleItems, o)
	}

	return nil
}

// RemoveProductG relationship.
// Sets o.R.Product to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle.
func (o *IncomeSaleItem) RemoveProductG(ctx context.Context, related *Product) error {
	return o.RemoveProduct(ctx, boil.GetContextDB(), related)
}

// RemoveProduct relationship.
// Sets o.R.Product to nil.
// Removes o from all passed in related items' relationships struct.
func (o *IncomeSaleItem) RemoveProduct(ctx context.Context, exec boil.ContextExecutor, related *Product) error {
	var err error

	queries.SetScanner(&o.ProductID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("product_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Product = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.IncomeSaleItems {
		if queries.Equal(o.ProductID, ri.ProductID) {
			continue
		}

		ln := len(related.R.IncomeSaleItems)
		if ln > 1 && i < ln-1 {
			related.R.IncomeSaleItems[i] = related.R.IncomeSaleItems[ln-1]
		}
		related.R.IncomeSaleItems = related.R.IncomeSaleItems[:ln-1]
		break
	}
	return nil
}

// SetIncomeSaleG of the incomeSaleItem to the related item.
// Sets o.R.IncomeSale to related.
// Adds o to related.R.IncomeSaleItems.
// Uses the global database handle.
func (o *IncomeSaleItem) SetIncomeSaleG(ctx context.Context, insert bool, related *IncomeSale) error {
	return o.SetIncomeSale(ctx, boil.GetContextDB(), insert, related)
}

// SetIncomeSale of the incomeSaleItem to the related item.
// Sets o.R.IncomeSale to related.
// Adds o to related.R.IncomeSaleItems.
func (o *IncomeSaleItem) SetIncomeSale(ctx context.Context, exec boil.ContextExecutor, insert bool, related *IncomeSale) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"income_sale_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"income_sale_id"}),
		strmangle.WhereClause("\"", "\"", 2, incomeSaleItemPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.IncomeSaleID, related.ID)
	if o.R == nil {
		o.R = &incomeSaleItemR{
			IncomeSale: related,
		}
	} else {
		o.R.IncomeSale = related
	}

	if related.R == nil {
		related.R = &incomeSaleR{
			IncomeSaleItems: IncomeSaleItemSlice{o},
		}
	} else {
		related.R.IncomeSaleItems = append(related.R.IncomeSaleItems, o)
	}

	return nil
}

// RemoveIncomeSaleG relationship.
// Sets o.R.IncomeSale to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle.
func (o *IncomeSaleItem) RemoveIncomeSaleG(ctx context.Context, related *IncomeSale) error {
	return o.RemoveIncomeSale(ctx, boil.GetContextDB(), related)
}

// RemoveIncomeSale relationship.
// Sets o.R.IncomeSale to nil.
// Removes o from all passed in related items' relationships struct.
func (o *IncomeSaleItem) RemoveIncomeSale(ctx context.Context, exec boil.ContextExecutor, related *IncomeSale) error {
	var err error

	queries.SetScanner(&o.IncomeSaleID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("income_sale_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.IncomeSale = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.IncomeSaleItems {
		if queries.Equal(o.IncomeSaleID, ri.IncomeSaleID) {
			continue
		}

		ln := len(related.R.IncomeSaleItems)
		if ln > 1 && i < ln-1 {
			related.R.IncomeSaleItems[i] = related.R.IncomeSaleItems[ln-1]
		}
		related.R.IncomeSaleItems = related.R.IncomeSaleItems[:ln-1]
		break
	}
	return nil
}

// IncomeSaleItems retrieves all the records using an executor.
func IncomeSaleItems(mods ...qm.QueryMod) incomeSaleItemQuery {
	mods = append(mods, qm.From("\"income_sale_items\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"income_sale_items\".*"})
	}

	return incomeSaleItemQuery{q}
}

// FindIncomeSaleItemG retrieves a single record by ID.
func FindIncomeSaleItemG(ctx context.Context, iD int64, selectCols ...string) (*IncomeSaleItem, error) {
	return FindIncomeSaleItem(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindIncomeSaleItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindIncomeSaleItem(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*IncomeSaleItem, error) {
	incomeSaleItemObj := &IncomeSaleItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"income_sale_items\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, incomeSaleItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "tenant: unable to select from income_sale_items")
	}

	if err = incomeSaleItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return incomeSaleItemObj, err
	}

	return incomeSaleItemObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *IncomeSaleItem) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *IncomeSaleItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("tenant: no income_sale_items provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incomeSaleItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	incomeSaleItemInsertCacheMut.RLock()
	cache, cached := incomeSaleItemInsertCache[key]
	incomeSaleItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			incomeSaleItemAllColumns,
			incomeSaleItemColumnsWithDefault,
			incomeSaleItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(incomeSaleItemType, incomeSaleItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(incomeSaleItemType, incomeSaleItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"income_sale_items\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"income_sale_items\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "tenant: unable to insert into income_sale_items")
	}

	if !cached {
		incomeSaleItemInsertCacheMut.Lock()
		incomeSaleItemInsertCache[key] = cache
		incomeSaleItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single IncomeSaleItem record using the global executor.
// See Update for more documentation.
func (o *IncomeSaleItem) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the IncomeSaleItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *IncomeSaleItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	incomeSaleItemUpdateCacheMut.RLock()
	cache, cached := incomeSaleItemUpdateCache[key]
	incomeSaleItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			incomeSaleItemAllColumns,
			incomeSaleItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("tenant: unable to update income_sale_items, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"income_sale_items\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, incomeSaleItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(incomeSaleItemType, incomeSaleItemMapping, append(wl, incomeSaleItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to update income_sale_items row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by update for income_sale_items")
	}

	if !cached {
		incomeSaleItemUpdateCacheMut.Lock()
		incomeSaleItemUpdateCache[key] = cache
		incomeSaleItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q incomeSaleItemQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q incomeSaleItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to update all for income_sale_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to retrieve rows affected for income_sale_items")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o IncomeSaleItemSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o IncomeSaleItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("tenant: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incomeSaleItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"income_sale_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, incomeSaleItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to update all in incomeSaleItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to retrieve rows affected all in update all incomeSaleItem")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *IncomeSaleItem) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *IncomeSaleItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("tenant: no income_sale_items provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incomeSaleItemColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	incomeSaleItemUpsertCacheMut.RLock()
	cache, cached := incomeSaleItemUpsertCache[key]
	incomeSaleItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			incomeSaleItemAllColumns,
			incomeSaleItemColumnsWithDefault,
			incomeSaleItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			incomeSaleItemAllColumns,
			incomeSaleItemPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("tenant: unable to upsert income_sale_items, could not build update column list")
		}

		ret := strmangle.SetComplement(incomeSaleItemAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(incomeSaleItemPrimaryKeyColumns) == 0 {
				return errors.New("tenant: unable to upsert income_sale_items, could not build conflict column list")
			}

			conflict = make([]string, len(incomeSaleItemPrimaryKeyColumns))
			copy(conflict, incomeSaleItemPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"income_sale_items\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(incomeSaleItemType, incomeSaleItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(incomeSaleItemType, incomeSaleItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "tenant: unable to upsert income_sale_items")
	}

	if !cached {
		incomeSaleItemUpsertCacheMut.Lock()
		incomeSaleItemUpsertCache[key] = cache
		incomeSaleItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single IncomeSaleItem record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *IncomeSaleItem) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single IncomeSaleItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *IncomeSaleItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("tenant: no IncomeSaleItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), incomeSaleItemPrimaryKeyMapping)
	sql := "DELETE FROM \"income_sale_items\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to delete from income_sale_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by delete for income_sale_items")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q incomeSaleItemQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q incomeSaleItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("tenant: no incomeSaleItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to delete all from income_sale_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by deleteall for income_sale_items")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o IncomeSaleItemSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o IncomeSaleItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(incomeSaleItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incomeSaleItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"income_sale_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incomeSaleItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to delete all from incomeSaleItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by deleteall for income_sale_items")
	}

	if len(incomeSaleItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *IncomeSaleItem) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("tenant: no IncomeSaleItem provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *IncomeSaleItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindIncomeSaleItem(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IncomeSaleItemSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("tenant: empty IncomeSaleItemSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IncomeSaleItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := IncomeSaleItemSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incomeSaleItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"income_sale_items\".* FROM \"income_sale_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incomeSaleItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "tenant: unable to reload all in IncomeSaleItemSlice")
	}

	*o = slice

	return nil
}

// IncomeSaleItemExistsG checks if the IncomeSaleItem row exists.
func IncomeSaleItemExistsG(ctx context.Context, iD int64) (bool, error) {
	return IncomeSaleItemExists(ctx, boil.GetContextDB(), iD)
}

// IncomeSaleItemExists checks if the IncomeSaleItem row exists.
func IncomeSaleItemExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"income_sale_items\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "tenant: unable to check if income_sale_items exists")
	}

	return exists, nil
}

// Exists checks if the IncomeSaleItem row exists.
func (o *IncomeSaleItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return IncomeSaleItemExists(ctx, exec, o.ID)
}
