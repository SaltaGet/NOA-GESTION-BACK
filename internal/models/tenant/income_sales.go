// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package tenant

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// IncomeSale is an object representing the database table.
type IncomeSale struct {
	ID             int64         `boil:"id" json:"id" toml:"id" yaml:"id"`
	PointSaleID    int64         `boil:"point_sale_id" json:"point_sale_id" toml:"point_sale_id" yaml:"point_sale_id"`
	MemberID       int64         `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	ClientID       int64         `boil:"client_id" json:"client_id" toml:"client_id" yaml:"client_id"`
	CashRegisterID int64         `boil:"cash_register_id" json:"cash_register_id" toml:"cash_register_id" yaml:"cash_register_id"`
	Subtotal       types.Decimal `boil:"subtotal" json:"subtotal" toml:"subtotal" yaml:"subtotal"`
	Discount       types.Decimal `boil:"discount" json:"discount" toml:"discount" yaml:"discount"`
	Type           string        `boil:"type" json:"type" toml:"type" yaml:"type"`
	Total          types.Decimal `boil:"total" json:"total" toml:"total" yaml:"total"`
	IsBudget       bool          `boil:"is_budget" json:"is_budget" toml:"is_budget" yaml:"is_budget"`
	InvoiceID      null.Int64    `boil:"invoice_id" json:"invoice_id,omitempty" toml:"invoice_id" yaml:"invoice_id,omitempty"`
	CreatedAt      time.Time     `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt      time.Time     `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *incomeSaleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L incomeSaleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var IncomeSaleColumns = struct {
	ID             string
	PointSaleID    string
	MemberID       string
	ClientID       string
	CashRegisterID string
	Subtotal       string
	Discount       string
	Type           string
	Total          string
	IsBudget       string
	InvoiceID      string
	CreatedAt      string
	UpdatedAt      string
}{
	ID:             "id",
	PointSaleID:    "point_sale_id",
	MemberID:       "member_id",
	ClientID:       "client_id",
	CashRegisterID: "cash_register_id",
	Subtotal:       "subtotal",
	Discount:       "discount",
	Type:           "type",
	Total:          "total",
	IsBudget:       "is_budget",
	InvoiceID:      "invoice_id",
	CreatedAt:      "created_at",
	UpdatedAt:      "updated_at",
}

var IncomeSaleTableColumns = struct {
	ID             string
	PointSaleID    string
	MemberID       string
	ClientID       string
	CashRegisterID string
	Subtotal       string
	Discount       string
	Type           string
	Total          string
	IsBudget       string
	InvoiceID      string
	CreatedAt      string
	UpdatedAt      string
}{
	ID:             "income_sales.id",
	PointSaleID:    "income_sales.point_sale_id",
	MemberID:       "income_sales.member_id",
	ClientID:       "income_sales.client_id",
	CashRegisterID: "income_sales.cash_register_id",
	Subtotal:       "income_sales.subtotal",
	Discount:       "income_sales.discount",
	Type:           "income_sales.type",
	Total:          "income_sales.total",
	IsBudget:       "income_sales.is_budget",
	InvoiceID:      "income_sales.invoice_id",
	CreatedAt:      "income_sales.created_at",
	UpdatedAt:      "income_sales.updated_at",
}

// Generated where

var IncomeSaleWhere = struct {
	ID             whereHelperint64
	PointSaleID    whereHelperint64
	MemberID       whereHelperint64
	ClientID       whereHelperint64
	CashRegisterID whereHelperint64
	Subtotal       whereHelpertypes_Decimal
	Discount       whereHelpertypes_Decimal
	Type           whereHelperstring
	Total          whereHelpertypes_Decimal
	IsBudget       whereHelperbool
	InvoiceID      whereHelpernull_Int64
	CreatedAt      whereHelpertime_Time
	UpdatedAt      whereHelpertime_Time
}{
	ID:             whereHelperint64{field: "\"income_sales\".\"id\""},
	PointSaleID:    whereHelperint64{field: "\"income_sales\".\"point_sale_id\""},
	MemberID:       whereHelperint64{field: "\"income_sales\".\"member_id\""},
	ClientID:       whereHelperint64{field: "\"income_sales\".\"client_id\""},
	CashRegisterID: whereHelperint64{field: "\"income_sales\".\"cash_register_id\""},
	Subtotal:       whereHelpertypes_Decimal{field: "\"income_sales\".\"subtotal\""},
	Discount:       whereHelpertypes_Decimal{field: "\"income_sales\".\"discount\""},
	Type:           whereHelperstring{field: "\"income_sales\".\"type\""},
	Total:          whereHelpertypes_Decimal{field: "\"income_sales\".\"total\""},
	IsBudget:       whereHelperbool{field: "\"income_sales\".\"is_budget\""},
	InvoiceID:      whereHelpernull_Int64{field: "\"income_sales\".\"invoice_id\""},
	CreatedAt:      whereHelpertime_Time{field: "\"income_sales\".\"created_at\""},
	UpdatedAt:      whereHelpertime_Time{field: "\"income_sales\".\"updated_at\""},
}

// IncomeSaleRels is where relationship names are stored.
var IncomeSaleRels = struct {
	CashRegister    string
	Client          string
	Invoice         string
	Member          string
	PointSale       string
	IncomeSaleItems string
	PayIncomes      string
}{
	CashRegister:    "CashRegister",
	Client:          "Client",
	Invoice:         "Invoice",
	Member:          "Member",
	PointSale:       "PointSale",
	IncomeSaleItems: "IncomeSaleItems",
	PayIncomes:      "PayIncomes",
}

// incomeSaleR is where relationships are stored.
type incomeSaleR struct {
	CashRegister    *CashRegister       `boil:"CashRegister" json:"CashRegister" toml:"CashRegister" yaml:"CashRegister"`
	Client          *Client             `boil:"Client" json:"Client" toml:"Client" yaml:"Client"`
	Invoice         *Invoice            `boil:"Invoice" json:"Invoice" toml:"Invoice" yaml:"Invoice"`
	Member          *Member             `boil:"Member" json:"Member" toml:"Member" yaml:"Member"`
	PointSale       *PointSale          `boil:"PointSale" json:"PointSale" toml:"PointSale" yaml:"PointSale"`
	IncomeSaleItems IncomeSaleItemSlice `boil:"IncomeSaleItems" json:"IncomeSaleItems" toml:"IncomeSaleItems" yaml:"IncomeSaleItems"`
	PayIncomes      PayIncomeSlice      `boil:"PayIncomes" json:"PayIncomes" toml:"PayIncomes" yaml:"PayIncomes"`
}

// NewStruct creates a new relationship struct
func (*incomeSaleR) NewStruct() *incomeSaleR {
	return &incomeSaleR{}
}

func (o *IncomeSale) GetCashRegister() *CashRegister {
	if o == nil {
		return nil
	}

	return o.R.GetCashRegister()
}

func (r *incomeSaleR) GetCashRegister() *CashRegister {
	if r == nil {
		return nil
	}

	return r.CashRegister
}

func (o *IncomeSale) GetClient() *Client {
	if o == nil {
		return nil
	}

	return o.R.GetClient()
}

func (r *incomeSaleR) GetClient() *Client {
	if r == nil {
		return nil
	}

	return r.Client
}

func (o *IncomeSale) GetInvoice() *Invoice {
	if o == nil {
		return nil
	}

	return o.R.GetInvoice()
}

func (r *incomeSaleR) GetInvoice() *Invoice {
	if r == nil {
		return nil
	}

	return r.Invoice
}

func (o *IncomeSale) GetMember() *Member {
	if o == nil {
		return nil
	}

	return o.R.GetMember()
}

func (r *incomeSaleR) GetMember() *Member {
	if r == nil {
		return nil
	}

	return r.Member
}

func (o *IncomeSale) GetPointSale() *PointSale {
	if o == nil {
		return nil
	}

	return o.R.GetPointSale()
}

func (r *incomeSaleR) GetPointSale() *PointSale {
	if r == nil {
		return nil
	}

	return r.PointSale
}

func (o *IncomeSale) GetIncomeSaleItems() IncomeSaleItemSlice {
	if o == nil {
		return nil
	}

	return o.R.GetIncomeSaleItems()
}

func (r *incomeSaleR) GetIncomeSaleItems() IncomeSaleItemSlice {
	if r == nil {
		return nil
	}

	return r.IncomeSaleItems
}

func (o *IncomeSale) GetPayIncomes() PayIncomeSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPayIncomes()
}

func (r *incomeSaleR) GetPayIncomes() PayIncomeSlice {
	if r == nil {
		return nil
	}

	return r.PayIncomes
}

// incomeSaleL is where Load methods for each relationship are stored.
type incomeSaleL struct{}

var (
	incomeSaleAllColumns            = []string{"id", "point_sale_id", "member_id", "client_id", "cash_register_id", "subtotal", "discount", "type", "total", "is_budget", "invoice_id", "created_at", "updated_at"}
	incomeSaleColumnsWithoutDefault = []string{"point_sale_id", "member_id", "client_id", "cash_register_id", "subtotal", "total"}
	incomeSaleColumnsWithDefault    = []string{"id", "discount", "type", "is_budget", "invoice_id", "created_at", "updated_at"}
	incomeSalePrimaryKeyColumns     = []string{"id"}
	incomeSaleGeneratedColumns      = []string{}
)

type (
	// IncomeSaleSlice is an alias for a slice of pointers to IncomeSale.
	// This should almost always be used instead of []IncomeSale.
	IncomeSaleSlice []*IncomeSale
	// IncomeSaleHook is the signature for custom IncomeSale hook methods
	IncomeSaleHook func(context.Context, boil.ContextExecutor, *IncomeSale) error

	incomeSaleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	incomeSaleType                 = reflect.TypeOf(&IncomeSale{})
	incomeSaleMapping              = queries.MakeStructMapping(incomeSaleType)
	incomeSalePrimaryKeyMapping, _ = queries.BindMapping(incomeSaleType, incomeSaleMapping, incomeSalePrimaryKeyColumns)
	incomeSaleInsertCacheMut       sync.RWMutex
	incomeSaleInsertCache          = make(map[string]insertCache)
	incomeSaleUpdateCacheMut       sync.RWMutex
	incomeSaleUpdateCache          = make(map[string]updateCache)
	incomeSaleUpsertCacheMut       sync.RWMutex
	incomeSaleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var incomeSaleAfterSelectMu sync.Mutex
var incomeSaleAfterSelectHooks []IncomeSaleHook

var incomeSaleBeforeInsertMu sync.Mutex
var incomeSaleBeforeInsertHooks []IncomeSaleHook
var incomeSaleAfterInsertMu sync.Mutex
var incomeSaleAfterInsertHooks []IncomeSaleHook

var incomeSaleBeforeUpdateMu sync.Mutex
var incomeSaleBeforeUpdateHooks []IncomeSaleHook
var incomeSaleAfterUpdateMu sync.Mutex
var incomeSaleAfterUpdateHooks []IncomeSaleHook

var incomeSaleBeforeDeleteMu sync.Mutex
var incomeSaleBeforeDeleteHooks []IncomeSaleHook
var incomeSaleAfterDeleteMu sync.Mutex
var incomeSaleAfterDeleteHooks []IncomeSaleHook

var incomeSaleBeforeUpsertMu sync.Mutex
var incomeSaleBeforeUpsertHooks []IncomeSaleHook
var incomeSaleAfterUpsertMu sync.Mutex
var incomeSaleAfterUpsertHooks []IncomeSaleHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *IncomeSale) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *IncomeSale) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *IncomeSale) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *IncomeSale) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *IncomeSale) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *IncomeSale) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *IncomeSale) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *IncomeSale) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *IncomeSale) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range incomeSaleAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddIncomeSaleHook registers your hook function for all future operations.
func AddIncomeSaleHook(hookPoint boil.HookPoint, incomeSaleHook IncomeSaleHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		incomeSaleAfterSelectMu.Lock()
		incomeSaleAfterSelectHooks = append(incomeSaleAfterSelectHooks, incomeSaleHook)
		incomeSaleAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		incomeSaleBeforeInsertMu.Lock()
		incomeSaleBeforeInsertHooks = append(incomeSaleBeforeInsertHooks, incomeSaleHook)
		incomeSaleBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		incomeSaleAfterInsertMu.Lock()
		incomeSaleAfterInsertHooks = append(incomeSaleAfterInsertHooks, incomeSaleHook)
		incomeSaleAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		incomeSaleBeforeUpdateMu.Lock()
		incomeSaleBeforeUpdateHooks = append(incomeSaleBeforeUpdateHooks, incomeSaleHook)
		incomeSaleBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		incomeSaleAfterUpdateMu.Lock()
		incomeSaleAfterUpdateHooks = append(incomeSaleAfterUpdateHooks, incomeSaleHook)
		incomeSaleAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		incomeSaleBeforeDeleteMu.Lock()
		incomeSaleBeforeDeleteHooks = append(incomeSaleBeforeDeleteHooks, incomeSaleHook)
		incomeSaleBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		incomeSaleAfterDeleteMu.Lock()
		incomeSaleAfterDeleteHooks = append(incomeSaleAfterDeleteHooks, incomeSaleHook)
		incomeSaleAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		incomeSaleBeforeUpsertMu.Lock()
		incomeSaleBeforeUpsertHooks = append(incomeSaleBeforeUpsertHooks, incomeSaleHook)
		incomeSaleBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		incomeSaleAfterUpsertMu.Lock()
		incomeSaleAfterUpsertHooks = append(incomeSaleAfterUpsertHooks, incomeSaleHook)
		incomeSaleAfterUpsertMu.Unlock()
	}
}

// OneG returns a single incomeSale record from the query using the global executor.
func (q incomeSaleQuery) OneG(ctx context.Context) (*IncomeSale, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single incomeSale record from the query.
func (q incomeSaleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*IncomeSale, error) {
	o := &IncomeSale{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "tenant: failed to execute a one query for income_sales")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all IncomeSale records from the query using the global executor.
func (q incomeSaleQuery) AllG(ctx context.Context) (IncomeSaleSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all IncomeSale records from the query.
func (q incomeSaleQuery) All(ctx context.Context, exec boil.ContextExecutor) (IncomeSaleSlice, error) {
	var o []*IncomeSale

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "tenant: failed to assign all query results to IncomeSale slice")
	}

	if len(incomeSaleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all IncomeSale records in the query using the global executor
func (q incomeSaleQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all IncomeSale records in the query.
func (q incomeSaleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to count income_sales rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q incomeSaleQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q incomeSaleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "tenant: failed to check if income_sales exists")
	}

	return count > 0, nil
}

// CashRegister pointed to by the foreign key.
func (o *IncomeSale) CashRegister(mods ...qm.QueryMod) cashRegisterQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CashRegisterID),
	}

	queryMods = append(queryMods, mods...)

	return CashRegisters(queryMods...)
}

// Client pointed to by the foreign key.
func (o *IncomeSale) Client(mods ...qm.QueryMod) clientQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ClientID),
	}

	queryMods = append(queryMods, mods...)

	return Clients(queryMods...)
}

// Invoice pointed to by the foreign key.
func (o *IncomeSale) Invoice(mods ...qm.QueryMod) invoiceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.InvoiceID),
	}

	queryMods = append(queryMods, mods...)

	return Invoices(queryMods...)
}

// Member pointed to by the foreign key.
func (o *IncomeSale) Member(mods ...qm.QueryMod) memberQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.MemberID),
	}

	queryMods = append(queryMods, mods...)

	return Members(queryMods...)
}

// PointSale pointed to by the foreign key.
func (o *IncomeSale) PointSale(mods ...qm.QueryMod) pointSaleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PointSaleID),
	}

	queryMods = append(queryMods, mods...)

	return PointSales(queryMods...)
}

// IncomeSaleItems retrieves all the income_sale_item's IncomeSaleItems with an executor.
func (o *IncomeSale) IncomeSaleItems(mods ...qm.QueryMod) incomeSaleItemQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"income_sale_items\".\"income_sale_id\"=?", o.ID),
	)

	return IncomeSaleItems(queryMods...)
}

// PayIncomes retrieves all the pay_income's PayIncomes with an executor.
func (o *IncomeSale) PayIncomes(mods ...qm.QueryMod) payIncomeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"pay_incomes\".\"income_sale_id\"=?", o.ID),
	)

	return PayIncomes(queryMods...)
}

// LoadCashRegister allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incomeSaleL) LoadCashRegister(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSale any, mods queries.Applicator) error {
	var slice []*IncomeSale
	var object *IncomeSale

	if singular {
		var ok bool
		object, ok = maybeIncomeSale.(*IncomeSale)
		if !ok {
			object = new(IncomeSale)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSale))
			}
		}
	} else {
		s, ok := maybeIncomeSale.(*[]*IncomeSale)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSale))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleR{}
		}
		args[object.CashRegisterID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleR{}
			}

			args[obj.CashRegisterID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cash_registers`),
		qm.WhereIn(`cash_registers.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CashRegister")
	}

	var resultSlice []*CashRegister
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CashRegister")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cash_registers")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cash_registers")
	}

	if len(cashRegisterAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CashRegister = foreign
		if foreign.R == nil {
			foreign.R = &cashRegisterR{}
		}
		foreign.R.IncomeSales = append(foreign.R.IncomeSales, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CashRegisterID == foreign.ID {
				local.R.CashRegister = foreign
				if foreign.R == nil {
					foreign.R = &cashRegisterR{}
				}
				foreign.R.IncomeSales = append(foreign.R.IncomeSales, local)
				break
			}
		}
	}

	return nil
}

// LoadClient allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incomeSaleL) LoadClient(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSale any, mods queries.Applicator) error {
	var slice []*IncomeSale
	var object *IncomeSale

	if singular {
		var ok bool
		object, ok = maybeIncomeSale.(*IncomeSale)
		if !ok {
			object = new(IncomeSale)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSale))
			}
		}
	} else {
		s, ok := maybeIncomeSale.(*[]*IncomeSale)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSale))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleR{}
		}
		args[object.ClientID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleR{}
			}

			args[obj.ClientID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`clients`),
		qm.WhereIn(`clients.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Client")
	}

	var resultSlice []*Client
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Client")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for clients")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for clients")
	}

	if len(clientAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Client = foreign
		if foreign.R == nil {
			foreign.R = &clientR{}
		}
		foreign.R.IncomeSales = append(foreign.R.IncomeSales, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ClientID == foreign.ID {
				local.R.Client = foreign
				if foreign.R == nil {
					foreign.R = &clientR{}
				}
				foreign.R.IncomeSales = append(foreign.R.IncomeSales, local)
				break
			}
		}
	}

	return nil
}

// LoadInvoice allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incomeSaleL) LoadInvoice(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSale any, mods queries.Applicator) error {
	var slice []*IncomeSale
	var object *IncomeSale

	if singular {
		var ok bool
		object, ok = maybeIncomeSale.(*IncomeSale)
		if !ok {
			object = new(IncomeSale)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSale))
			}
		}
	} else {
		s, ok := maybeIncomeSale.(*[]*IncomeSale)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSale))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleR{}
		}
		if !queries.IsNil(object.InvoiceID) {
			args[object.InvoiceID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleR{}
			}

			if !queries.IsNil(obj.InvoiceID) {
				args[obj.InvoiceID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`invoices`),
		qm.WhereIn(`invoices.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Invoice")
	}

	var resultSlice []*Invoice
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Invoice")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for invoices")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for invoices")
	}

	if len(invoiceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Invoice = foreign
		if foreign.R == nil {
			foreign.R = &invoiceR{}
		}
		foreign.R.IncomeSales = append(foreign.R.IncomeSales, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.InvoiceID, foreign.ID) {
				local.R.Invoice = foreign
				if foreign.R == nil {
					foreign.R = &invoiceR{}
				}
				foreign.R.IncomeSales = append(foreign.R.IncomeSales, local)
				break
			}
		}
	}

	return nil
}

// LoadMember allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incomeSaleL) LoadMember(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSale any, mods queries.Applicator) error {
	var slice []*IncomeSale
	var object *IncomeSale

	if singular {
		var ok bool
		object, ok = maybeIncomeSale.(*IncomeSale)
		if !ok {
			object = new(IncomeSale)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSale))
			}
		}
	} else {
		s, ok := maybeIncomeSale.(*[]*IncomeSale)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSale))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleR{}
		}
		args[object.MemberID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleR{}
			}

			args[obj.MemberID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`members`),
		qm.WhereIn(`members.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Member")
	}

	var resultSlice []*Member
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Member")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for members")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for members")
	}

	if len(memberAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Member = foreign
		if foreign.R == nil {
			foreign.R = &memberR{}
		}
		foreign.R.IncomeSales = append(foreign.R.IncomeSales, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MemberID == foreign.ID {
				local.R.Member = foreign
				if foreign.R == nil {
					foreign.R = &memberR{}
				}
				foreign.R.IncomeSales = append(foreign.R.IncomeSales, local)
				break
			}
		}
	}

	return nil
}

// LoadPointSale allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (incomeSaleL) LoadPointSale(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSale any, mods queries.Applicator) error {
	var slice []*IncomeSale
	var object *IncomeSale

	if singular {
		var ok bool
		object, ok = maybeIncomeSale.(*IncomeSale)
		if !ok {
			object = new(IncomeSale)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSale))
			}
		}
	} else {
		s, ok := maybeIncomeSale.(*[]*IncomeSale)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSale))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleR{}
		}
		args[object.PointSaleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleR{}
			}

			args[obj.PointSaleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`point_sales`),
		qm.WhereIn(`point_sales.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PointSale")
	}

	var resultSlice []*PointSale
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PointSale")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for point_sales")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for point_sales")
	}

	if len(pointSaleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PointSale = foreign
		if foreign.R == nil {
			foreign.R = &pointSaleR{}
		}
		foreign.R.IncomeSales = append(foreign.R.IncomeSales, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PointSaleID == foreign.ID {
				local.R.PointSale = foreign
				if foreign.R == nil {
					foreign.R = &pointSaleR{}
				}
				foreign.R.IncomeSales = append(foreign.R.IncomeSales, local)
				break
			}
		}
	}

	return nil
}

// LoadIncomeSaleItems allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (incomeSaleL) LoadIncomeSaleItems(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSale any, mods queries.Applicator) error {
	var slice []*IncomeSale
	var object *IncomeSale

	if singular {
		var ok bool
		object, ok = maybeIncomeSale.(*IncomeSale)
		if !ok {
			object = new(IncomeSale)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSale))
			}
		}
	} else {
		s, ok := maybeIncomeSale.(*[]*IncomeSale)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSale))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`income_sale_items`),
		qm.WhereIn(`income_sale_items.income_sale_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load income_sale_items")
	}

	var resultSlice []*IncomeSaleItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice income_sale_items")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on income_sale_items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for income_sale_items")
	}

	if len(incomeSaleItemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.IncomeSaleItems = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &incomeSaleItemR{}
			}
			foreign.R.IncomeSale = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.IncomeSaleID) {
				local.R.IncomeSaleItems = append(local.R.IncomeSaleItems, foreign)
				if foreign.R == nil {
					foreign.R = &incomeSaleItemR{}
				}
				foreign.R.IncomeSale = local
				break
			}
		}
	}

	return nil
}

// LoadPayIncomes allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (incomeSaleL) LoadPayIncomes(ctx context.Context, e boil.ContextExecutor, singular bool, maybeIncomeSale any, mods queries.Applicator) error {
	var slice []*IncomeSale
	var object *IncomeSale

	if singular {
		var ok bool
		object, ok = maybeIncomeSale.(*IncomeSale)
		if !ok {
			object = new(IncomeSale)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeIncomeSale))
			}
		}
	} else {
		s, ok := maybeIncomeSale.(*[]*IncomeSale)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeIncomeSale)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeIncomeSale))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &incomeSaleR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &incomeSaleR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`pay_incomes`),
		qm.WhereIn(`pay_incomes.income_sale_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load pay_incomes")
	}

	var resultSlice []*PayIncome
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice pay_incomes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on pay_incomes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for pay_incomes")
	}

	if len(payIncomeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PayIncomes = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &payIncomeR{}
			}
			foreign.R.IncomeSale = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.IncomeSaleID {
				local.R.PayIncomes = append(local.R.PayIncomes, foreign)
				if foreign.R == nil {
					foreign.R = &payIncomeR{}
				}
				foreign.R.IncomeSale = local
				break
			}
		}
	}

	return nil
}

// SetCashRegisterG of the incomeSale to the related item.
// Sets o.R.CashRegister to related.
// Adds o to related.R.IncomeSales.
// Uses the global database handle.
func (o *IncomeSale) SetCashRegisterG(ctx context.Context, insert bool, related *CashRegister) error {
	return o.SetCashRegister(ctx, boil.GetContextDB(), insert, related)
}

// SetCashRegister of the incomeSale to the related item.
// Sets o.R.CashRegister to related.
// Adds o to related.R.IncomeSales.
func (o *IncomeSale) SetCashRegister(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CashRegister) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"income_sales\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"cash_register_id"}),
		strmangle.WhereClause("\"", "\"", 2, incomeSalePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CashRegisterID = related.ID
	if o.R == nil {
		o.R = &incomeSaleR{
			CashRegister: related,
		}
	} else {
		o.R.CashRegister = related
	}

	if related.R == nil {
		related.R = &cashRegisterR{
			IncomeSales: IncomeSaleSlice{o},
		}
	} else {
		related.R.IncomeSales = append(related.R.IncomeSales, o)
	}

	return nil
}

// SetClientG of the incomeSale to the related item.
// Sets o.R.Client to related.
// Adds o to related.R.IncomeSales.
// Uses the global database handle.
func (o *IncomeSale) SetClientG(ctx context.Context, insert bool, related *Client) error {
	return o.SetClient(ctx, boil.GetContextDB(), insert, related)
}

// SetClient of the incomeSale to the related item.
// Sets o.R.Client to related.
// Adds o to related.R.IncomeSales.
func (o *IncomeSale) SetClient(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Client) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"income_sales\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"client_id"}),
		strmangle.WhereClause("\"", "\"", 2, incomeSalePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ClientID = related.ID
	if o.R == nil {
		o.R = &incomeSaleR{
			Client: related,
		}
	} else {
		o.R.Client = related
	}

	if related.R == nil {
		related.R = &clientR{
			IncomeSales: IncomeSaleSlice{o},
		}
	} else {
		related.R.IncomeSales = append(related.R.IncomeSales, o)
	}

	return nil
}

// SetInvoiceG of the incomeSale to the related item.
// Sets o.R.Invoice to related.
// Adds o to related.R.IncomeSales.
// Uses the global database handle.
func (o *IncomeSale) SetInvoiceG(ctx context.Context, insert bool, related *Invoice) error {
	return o.SetInvoice(ctx, boil.GetContextDB(), insert, related)
}

// SetInvoice of the incomeSale to the related item.
// Sets o.R.Invoice to related.
// Adds o to related.R.IncomeSales.
func (o *IncomeSale) SetInvoice(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Invoice) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"income_sales\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"invoice_id"}),
		strmangle.WhereClause("\"", "\"", 2, incomeSalePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.InvoiceID, related.ID)
	if o.R == nil {
		o.R = &incomeSaleR{
			Invoice: related,
		}
	} else {
		o.R.Invoice = related
	}

	if related.R == nil {
		related.R = &invoiceR{
			IncomeSales: IncomeSaleSlice{o},
		}
	} else {
		related.R.IncomeSales = append(related.R.IncomeSales, o)
	}

	return nil
}

// RemoveInvoiceG relationship.
// Sets o.R.Invoice to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle.
func (o *IncomeSale) RemoveInvoiceG(ctx context.Context, related *Invoice) error {
	return o.RemoveInvoice(ctx, boil.GetContextDB(), related)
}

// RemoveInvoice relationship.
// Sets o.R.Invoice to nil.
// Removes o from all passed in related items' relationships struct.
func (o *IncomeSale) RemoveInvoice(ctx context.Context, exec boil.ContextExecutor, related *Invoice) error {
	var err error

	queries.SetScanner(&o.InvoiceID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("invoice_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Invoice = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.IncomeSales {
		if queries.Equal(o.InvoiceID, ri.InvoiceID) {
			continue
		}

		ln := len(related.R.IncomeSales)
		if ln > 1 && i < ln-1 {
			related.R.IncomeSales[i] = related.R.IncomeSales[ln-1]
		}
		related.R.IncomeSales = related.R.IncomeSales[:ln-1]
		break
	}
	return nil
}

// SetMemberG of the incomeSale to the related item.
// Sets o.R.Member to related.
// Adds o to related.R.IncomeSales.
// Uses the global database handle.
func (o *IncomeSale) SetMemberG(ctx context.Context, insert bool, related *Member) error {
	return o.SetMember(ctx, boil.GetContextDB(), insert, related)
}

// SetMember of the incomeSale to the related item.
// Sets o.R.Member to related.
// Adds o to related.R.IncomeSales.
func (o *IncomeSale) SetMember(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Member) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"income_sales\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"member_id"}),
		strmangle.WhereClause("\"", "\"", 2, incomeSalePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MemberID = related.ID
	if o.R == nil {
		o.R = &incomeSaleR{
			Member: related,
		}
	} else {
		o.R.Member = related
	}

	if related.R == nil {
		related.R = &memberR{
			IncomeSales: IncomeSaleSlice{o},
		}
	} else {
		related.R.IncomeSales = append(related.R.IncomeSales, o)
	}

	return nil
}

// SetPointSaleG of the incomeSale to the related item.
// Sets o.R.PointSale to related.
// Adds o to related.R.IncomeSales.
// Uses the global database handle.
func (o *IncomeSale) SetPointSaleG(ctx context.Context, insert bool, related *PointSale) error {
	return o.SetPointSale(ctx, boil.GetContextDB(), insert, related)
}

// SetPointSale of the incomeSale to the related item.
// Sets o.R.PointSale to related.
// Adds o to related.R.IncomeSales.
func (o *IncomeSale) SetPointSale(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PointSale) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"income_sales\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"point_sale_id"}),
		strmangle.WhereClause("\"", "\"", 2, incomeSalePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PointSaleID = related.ID
	if o.R == nil {
		o.R = &incomeSaleR{
			PointSale: related,
		}
	} else {
		o.R.PointSale = related
	}

	if related.R == nil {
		related.R = &pointSaleR{
			IncomeSales: IncomeSaleSlice{o},
		}
	} else {
		related.R.IncomeSales = append(related.R.IncomeSales, o)
	}

	return nil
}

// AddIncomeSaleItemsG adds the given related objects to the existing relationships
// of the income_sale, optionally inserting them as new records.
// Appends related to o.R.IncomeSaleItems.
// Sets related.R.IncomeSale appropriately.
// Uses the global database handle.
func (o *IncomeSale) AddIncomeSaleItemsG(ctx context.Context, insert bool, related ...*IncomeSaleItem) error {
	return o.AddIncomeSaleItems(ctx, boil.GetContextDB(), insert, related...)
}

// AddIncomeSaleItems adds the given related objects to the existing relationships
// of the income_sale, optionally inserting them as new records.
// Appends related to o.R.IncomeSaleItems.
// Sets related.R.IncomeSale appropriately.
func (o *IncomeSale) AddIncomeSaleItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*IncomeSaleItem) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.IncomeSaleID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"income_sale_items\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"income_sale_id"}),
				strmangle.WhereClause("\"", "\"", 2, incomeSaleItemPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.IncomeSaleID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &incomeSaleR{
			IncomeSaleItems: related,
		}
	} else {
		o.R.IncomeSaleItems = append(o.R.IncomeSaleItems, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &incomeSaleItemR{
				IncomeSale: o,
			}
		} else {
			rel.R.IncomeSale = o
		}
	}
	return nil
}

// SetIncomeSaleItemsG removes all previously related items of the
// income_sale replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.IncomeSale's IncomeSaleItems accordingly.
// Replaces o.R.IncomeSaleItems with related.
// Sets related.R.IncomeSale's IncomeSaleItems accordingly.
// Uses the global database handle.
func (o *IncomeSale) SetIncomeSaleItemsG(ctx context.Context, insert bool, related ...*IncomeSaleItem) error {
	return o.SetIncomeSaleItems(ctx, boil.GetContextDB(), insert, related...)
}

// SetIncomeSaleItems removes all previously related items of the
// income_sale replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.IncomeSale's IncomeSaleItems accordingly.
// Replaces o.R.IncomeSaleItems with related.
// Sets related.R.IncomeSale's IncomeSaleItems accordingly.
func (o *IncomeSale) SetIncomeSaleItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*IncomeSaleItem) error {
	query := "update \"income_sale_items\" set \"income_sale_id\" = null where \"income_sale_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.IncomeSaleItems {
			queries.SetScanner(&rel.IncomeSaleID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.IncomeSale = nil
		}
		o.R.IncomeSaleItems = nil
	}

	return o.AddIncomeSaleItems(ctx, exec, insert, related...)
}

// RemoveIncomeSaleItemsG relationships from objects passed in.
// Removes related items from R.IncomeSaleItems (uses pointer comparison, removal does not keep order)
// Sets related.R.IncomeSale.
// Uses the global database handle.
func (o *IncomeSale) RemoveIncomeSaleItemsG(ctx context.Context, related ...*IncomeSaleItem) error {
	return o.RemoveIncomeSaleItems(ctx, boil.GetContextDB(), related...)
}

// RemoveIncomeSaleItems relationships from objects passed in.
// Removes related items from R.IncomeSaleItems (uses pointer comparison, removal does not keep order)
// Sets related.R.IncomeSale.
func (o *IncomeSale) RemoveIncomeSaleItems(ctx context.Context, exec boil.ContextExecutor, related ...*IncomeSaleItem) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.IncomeSaleID, nil)
		if rel.R != nil {
			rel.R.IncomeSale = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("income_sale_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.IncomeSaleItems {
			if rel != ri {
				continue
			}

			ln := len(o.R.IncomeSaleItems)
			if ln > 1 && i < ln-1 {
				o.R.IncomeSaleItems[i] = o.R.IncomeSaleItems[ln-1]
			}
			o.R.IncomeSaleItems = o.R.IncomeSaleItems[:ln-1]
			break
		}
	}

	return nil
}

// AddPayIncomesG adds the given related objects to the existing relationships
// of the income_sale, optionally inserting them as new records.
// Appends related to o.R.PayIncomes.
// Sets related.R.IncomeSale appropriately.
// Uses the global database handle.
func (o *IncomeSale) AddPayIncomesG(ctx context.Context, insert bool, related ...*PayIncome) error {
	return o.AddPayIncomes(ctx, boil.GetContextDB(), insert, related...)
}

// AddPayIncomes adds the given related objects to the existing relationships
// of the income_sale, optionally inserting them as new records.
// Appends related to o.R.PayIncomes.
// Sets related.R.IncomeSale appropriately.
func (o *IncomeSale) AddPayIncomes(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PayIncome) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.IncomeSaleID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"pay_incomes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"income_sale_id"}),
				strmangle.WhereClause("\"", "\"", 2, payIncomePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.IncomeSaleID = o.ID
		}
	}

	if o.R == nil {
		o.R = &incomeSaleR{
			PayIncomes: related,
		}
	} else {
		o.R.PayIncomes = append(o.R.PayIncomes, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &payIncomeR{
				IncomeSale: o,
			}
		} else {
			rel.R.IncomeSale = o
		}
	}
	return nil
}

// IncomeSales retrieves all the records using an executor.
func IncomeSales(mods ...qm.QueryMod) incomeSaleQuery {
	mods = append(mods, qm.From("\"income_sales\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"income_sales\".*"})
	}

	return incomeSaleQuery{q}
}

// FindIncomeSaleG retrieves a single record by ID.
func FindIncomeSaleG(ctx context.Context, iD int64, selectCols ...string) (*IncomeSale, error) {
	return FindIncomeSale(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindIncomeSale retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindIncomeSale(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*IncomeSale, error) {
	incomeSaleObj := &IncomeSale{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"income_sales\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, incomeSaleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "tenant: unable to select from income_sales")
	}

	if err = incomeSaleObj.doAfterSelectHooks(ctx, exec); err != nil {
		return incomeSaleObj, err
	}

	return incomeSaleObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *IncomeSale) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *IncomeSale) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("tenant: no income_sales provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incomeSaleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	incomeSaleInsertCacheMut.RLock()
	cache, cached := incomeSaleInsertCache[key]
	incomeSaleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			incomeSaleAllColumns,
			incomeSaleColumnsWithDefault,
			incomeSaleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(incomeSaleType, incomeSaleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(incomeSaleType, incomeSaleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"income_sales\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"income_sales\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "tenant: unable to insert into income_sales")
	}

	if !cached {
		incomeSaleInsertCacheMut.Lock()
		incomeSaleInsertCache[key] = cache
		incomeSaleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single IncomeSale record using the global executor.
// See Update for more documentation.
func (o *IncomeSale) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the IncomeSale.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *IncomeSale) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	incomeSaleUpdateCacheMut.RLock()
	cache, cached := incomeSaleUpdateCache[key]
	incomeSaleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			incomeSaleAllColumns,
			incomeSalePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("tenant: unable to update income_sales, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"income_sales\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, incomeSalePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(incomeSaleType, incomeSaleMapping, append(wl, incomeSalePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to update income_sales row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by update for income_sales")
	}

	if !cached {
		incomeSaleUpdateCacheMut.Lock()
		incomeSaleUpdateCache[key] = cache
		incomeSaleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q incomeSaleQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q incomeSaleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to update all for income_sales")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to retrieve rows affected for income_sales")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o IncomeSaleSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o IncomeSaleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("tenant: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incomeSalePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"income_sales\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, incomeSalePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to update all in incomeSale slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to retrieve rows affected all in update all incomeSale")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *IncomeSale) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *IncomeSale) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("tenant: no income_sales provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(incomeSaleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	incomeSaleUpsertCacheMut.RLock()
	cache, cached := incomeSaleUpsertCache[key]
	incomeSaleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			incomeSaleAllColumns,
			incomeSaleColumnsWithDefault,
			incomeSaleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			incomeSaleAllColumns,
			incomeSalePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("tenant: unable to upsert income_sales, could not build update column list")
		}

		ret := strmangle.SetComplement(incomeSaleAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(incomeSalePrimaryKeyColumns) == 0 {
				return errors.New("tenant: unable to upsert income_sales, could not build conflict column list")
			}

			conflict = make([]string, len(incomeSalePrimaryKeyColumns))
			copy(conflict, incomeSalePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"income_sales\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(incomeSaleType, incomeSaleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(incomeSaleType, incomeSaleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "tenant: unable to upsert income_sales")
	}

	if !cached {
		incomeSaleUpsertCacheMut.Lock()
		incomeSaleUpsertCache[key] = cache
		incomeSaleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single IncomeSale record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *IncomeSale) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single IncomeSale record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *IncomeSale) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("tenant: no IncomeSale provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), incomeSalePrimaryKeyMapping)
	sql := "DELETE FROM \"income_sales\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to delete from income_sales")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by delete for income_sales")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q incomeSaleQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q incomeSaleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("tenant: no incomeSaleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to delete all from income_sales")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by deleteall for income_sales")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o IncomeSaleSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o IncomeSaleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(incomeSaleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incomeSalePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"income_sales\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incomeSalePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "tenant: unable to delete all from incomeSale slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "tenant: failed to get rows affected by deleteall for income_sales")
	}

	if len(incomeSaleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *IncomeSale) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("tenant: no IncomeSale provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *IncomeSale) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindIncomeSale(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IncomeSaleSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("tenant: empty IncomeSaleSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IncomeSaleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := IncomeSaleSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), incomeSalePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"income_sales\".* FROM \"income_sales\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, incomeSalePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "tenant: unable to reload all in IncomeSaleSlice")
	}

	*o = slice

	return nil
}

// IncomeSaleExistsG checks if the IncomeSale row exists.
func IncomeSaleExistsG(ctx context.Context, iD int64) (bool, error) {
	return IncomeSaleExists(ctx, boil.GetContextDB(), iD)
}

// IncomeSaleExists checks if the IncomeSale row exists.
func IncomeSaleExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"income_sales\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "tenant: unable to check if income_sales exists")
	}

	return exists, nil
}

// Exists checks if the IncomeSale row exists.
func (o *IncomeSale) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return IncomeSaleExists(ctx, exec, o.ID)
}
