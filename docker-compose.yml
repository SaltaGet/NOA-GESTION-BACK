services:
  # db:
  #   image: mariadb:11
  #   container_name: mariadb-noa
  #   ports:
  #     - "3308:3306"
  #   env_file:
  #     - .env
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
  #     - MYSQL_DATABASE=${DB_USER}
  #   volumes:
  #     # - ./databases:/var/lib/mysql
  #     - db-data:/var/lib/mysql
  #     - ./docker/mariadb/mariadb.cnf:/etc/mysql/mariadb.conf.d/99-custom.cnf:ro
  #   healthcheck:
  #     test: [ "CMD-SHELL", "mysqladmin ping -h localhost -u${DB_USER} -p${DB_PASSWORD} || exit 1" ]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 10
  #     start_period: 30s
  #   restart: unless-stopped
  #   networks:
  #     - monitoring

  db:
    image: postgres:16-alpine
    container_name: postgres-noa
    ports:
      - "5432:5432"
    env_file:
      - .env
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - db-data-postgres:/var/lib/postgresql/data
      # Si necesitas tunear la config, Postgres usa postgresql.conf
      - ./conf/postgres/my-postgres.conf:/etc/postgresql/postgresql.conf
    command: >
      postgres  -c config_file=/etc/postgresql/postgresql.conf
    healthcheck:
      # Usamos pg_isready para verificar si el servidor acepta conexiones
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      - monitoring

  front:
    image: picohbk/noagestionfront:1.0.0.28
    container_name: noagestion-front
    ports:
      - "5000:80"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - monitoring
    restart: unless-stopped

  api:
    image: danielmchachagua/noagestionback:1.0.0
    container_name: noa-gestion
    ports:
      - "3000:3000"
      - "50051:50051"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    environment:
      - NATS_URL=nats://${NATS_USER}:${NATS_PASSWORD}@noa-nats:4222
    restart: unless-stopped
    volumes:
      - ./backups:/app/backups
    networks:
      - default
      - monitoring
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3000/api/health" ]
      interval: 10s
      timeout: 3s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ecommerce:
  #   image: danielmchachagua/ecommercenoagestionback:0.1.21
  #   container_name: noa-ecommerce
  #   ports:
  #     - "3001:3001"
  #   env_file:
  #     - .env
  #   depends_on:
  #     api:
  #       condition: service_healthy
  #   restart: unless-stopped
  #   volumes:
  #     - ./backups:/app/backups
  #   networks:
  #     - default
  #     - monitoring
  #   logging:
  #     driver: json-file
  #     options:
  #       max-size: "10m"
  #       max-file: "3"

  email-ms:
    image: danielmchachagua/noagestionemail:1.0.0
    container_name: noa-email
    env_file:
      - .email.env
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - ./backups:/app/backups
    networks:
      - default
      - monitoring
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  nats:
    image: nats:2.10-alpine
    container_name: noa-nats
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "4222:4222"
      - "8222:8222"
    command: "-js -sd /data -m 8222 --user ${NATS_USER} --pass ${NATS_PASSWORD}"
    volumes:
      - nats-data:/data
    networks:
      - default
      - monitoring

  redis:
    image: redis:7-alpine
    container_name: noa_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - monitoring
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 3s
      retries: 5

networks:
  monitoring:
    name: monitoring

volumes:
  db-data-postgres:
  redis_data:
    driver: local
  nats-data:
  netdataconfig:
  netdatalib:
  netdatacache:

